import Registrations from '@/components/registrations';  // Lowercase 'r' if file is registrations.tsx

export default function RegistrationsPage() {
  return <Registrations />;  // Add this; check Registrations.tsx has content
}