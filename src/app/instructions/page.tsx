"use client";  // Matches client-side needs

import Instructions from '@/components/instructions';  // Use lowercase to match file name

export default function InstructionsPage() {
  return <Instructions />;
}